name: Website

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Enable GitHub Pages for this repository if it is not yet (source = GitHub Actions)
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: 17 }
      - uses: gradle/actions/setup-gradle@v4
      - name: Export app content (unit test writes app/build/site-content)
        run: ./gradlew --no-daemon testDebugUnitTest --tests 'cz.janek.vineyardlog.site.ContentExportTest'
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: pip install jinja2
      - run: python3 web/build_site.py
      - uses: actions/upload-pages-artifact@v3
        with: { path: web/dist }
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
